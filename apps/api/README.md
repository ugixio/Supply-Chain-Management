# api  (scaffold — NestJS + GraphQL)

NestJS modular monolith: one module per department (ADR-0023), code-first GraphQL
(ADR-0025). Stage A exposes the knowledge-graph read model (P3). Outermost ring.
