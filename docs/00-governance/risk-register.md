---
id: risk-register
title: "Risk Register"
type: governance
owner: orchestrator
status: draft
since: 2026-07-19
updated: 2026-08-03
relations:
  - { type: part-of, target: index-governance }
  - { type: governed-by, target: knowledge-architecture }
---
# Risk Register

> The single home for **identified risks** — technical, security, delivery and knowledge
> risks that are accepted-for-now rather than fixed-now (ADR-0012). A row makes the
> exposure visible, owned and reviewable; it never substitutes for fixing what a hard
> rule forbids. Rows are appended and updated in status; closed risks stay listed
> (mitigated / retired), never deleted.

## How to use

- Anyone may PROPOSE a risk; the owner accepts it into the register.
- Every risk has exactly one owner and a **review trigger** (an event or date that forces
  a re-look). A risk without a trigger is a worry, not a register entry.
- A risk that hardens into policy becomes a rule (`rule.md`) or a decision (ADR), and the
  row links to it. A risk deliberately left open cites the reasoning.
- Score likelihood and impact coarsely (`low | medium | high`) — the register ranks
  exposures; it does not pretend precision.

## Register

> **Eight closed rows live in [risk-register-archive.md](risk-register-archive.md)** — moved
> 2026-08-03 when the `reviewing-the-estate` load set reached its ceiling, whose declared answer was
> to archive rather than raise. Nothing was deleted: a closed risk stays listed so old references
> resolve. Seven of the eight closed on the same axis — ADR-0037 deleting the code they were about.

| # | Risk | Likelihood | Impact | Owner | Mitigation / response | Status | Review trigger |
|---|---|---|---|---|---|---|---|
| 3 | Default branch on GitHub is `claude/bold-cannon-l7wtso`, not a stable main line | high | medium | human | **Given a shape 2026-08-02 by ADR-0040 / ENG-R11**, after measuring what the note had left vague: all 22 pull requests target that session-generated name, no `main` exists, and three branches merged in July (`feat/context-skeleton`, `fix/verify-rule-id-regex`, `feat/per-department-rules`) are still on the remote carrying **0 commits not already in the base** — dead refs that invite a fresh session to branch from a July snapshot. `main` has been created at the identical tip, so the two never diverge. **Two steps are the owner's and cannot be done from a session:** flipping the default branch (a repository setting with no API surface here) and deleting the four stale refs (this environment's git proxy refuses `delete` with HTTP 403). The risk closes when the default is `main`. **CLOSED 2026-08-03, both owner steps done.** `git remote show origin` reports `HEAD branch: main`, and `git ls-remote --heads` returns exactly two refs — `main` and the current work branch. All four dead refs are gone, each verified at **0 commits not already in `main`** before deletion, so no history was discarded. PR #32 merged into `main` with a merge commit (ENG-R11.6), and the work branch was restarted from it, which is the first time the ENG-R11 cycle ran end to end. **The proxy limit stands and is worth keeping written down:** a session still cannot delete a ref here, so branch hygiene is an owner step by construction, not by neglect. | closed | any new long-lived branch; a work branch that outlives its merge |
| 5 | Gate G8 (English-only) is not machine-checked; prose drift can enter unnoticed | medium | low | orchestrator | **MITIGATED 2026-07-30 — G8 is machine-checked now.** `tools/verify.py` screens every tracked Markdown file for non-English function words and fails the gate on a hit; it caught its own author within two days, rejecting an improvement-register entry written in Spanish. The residual is the class, not the instances: a screen over a word list finds prose in the languages it lists, and a fluent non-English sentence built from words outside that list still passes. Same shape as #11 and #13 — a mechanical check narrows a risk without closing it. | mitigated | a violation that passes the screen; a new language appearing in the estate |
| 11 | **No gate can tell a standard from a plausible-looking invention.** G10 checks that a source is *cited*, not that the content matches it — a number copied from a textbook example reads exactly like a regulation. This is the residual of ADR-0037, and it is how the original problem started. | medium | high | orchestrator | the inclusion test at the head of `CLAUDE.md`; the anti-states in `30-foundation/scm-core/rule.md` as the reviewer's checklist; every node names its source so a claim can be checked against it | open | any new rule or concept node; any citation that cannot be followed to a published document |
| 12 | **A citation can stop matching its source without anything changing in this repository.** The law moves; the document does not. C5 found `REGULATORY_FRAMEWORK.md` publishing the superseded CSDDD phase-in, a penalty cap that had been reduced from 5% to 3%, and no record that the EU-wide civil liability regime had been deleted — all while every gate stayed green, because a gate can check that a source is *cited*, never that the source still says it. | high | high | orchestrator | a **verification date per entry** in `docs/standards/REGULATORY_FRAMEWORK.md`, so staleness is visible rather than invisible; the EU entries flagged as the shortest half-life; regulatory nodes carry their own verified-on date | open | any entry older than a year; any EU sustainability instrument, which have changed three times in eighteen months |
| 13 | **The agent and skill layer states policy as law, and no gate reads it.** `.claude/skills/**` publishes KPI *target* tables — `OTD ≥ 95%`, `Fill Rate ≥ 98%`, `Inventory Accuracy ≥ 99.5%`, `CoPQ < 2% of revenue`, `NCR closure ≥ 95% in 30 days` — and `.claude/commands/**` instructs a review of application code that ADR-0037 deleted. This is the same defect ADR-0037 corrected in `docs/`, in the one place that is loaded into every session's working set rather than read on demand. Two files show a sweep started there and stopped (`procurement` row 28, `quality-management` row 30), which is how it survived. | high | high | orchestrator | found and scoped by the 2026-07-29 file-by-file review; G8/G11/G12/G13 now read every tracked Markdown file including `.claude/**`, so the *citation* classes are covered — **swept 2026-07-29**: all fourteen department KPI tables rewritten from a level column into the decision plus what constrains it, the retired approval-threshold rule removed from the procurement code sample, the invented status vocabulary named as a project's own, the XYZ CV bands and the CMMI score bands removed, and all four commands reframed to review a *project's* code against this context. The residual is the class, not the instances: no gate can distinguish a target from a definition, so the next skill file added can reintroduce it. | mitigated | any new skill file; any KPI table anywhere in the estate; the next session that loads a department skill |
| 14 | **The rollup cascade would silently corrupt any *level* metric.** `samples_1m` computes `sumState`, `minState`, `maxState` and a p95 per (project, metric, minute) — correct for a flow, invalid for a level. A backlog of 40 read every ten seconds sums to 240 in the 1-minute bucket: six times reality, in range, with nothing failing and no gate able to see it. Found on 2026-08-01 while modelling the warehouse indicators (CPT-0163/0164), which include three levels. No level metric is ingested today, so the exposure is latent — and it becomes live the moment one is, which is exactly when nobody will be looking for it. | medium | high | orchestrator | **MITIGATED 2026-08-03 by M2b, structurally rather than by documentation.** `db/clickhouse/migrations/0006_metric_kind.sql` adds `kind` to `samples` and to all three rollups, and splits the read surface: `telemetry.levels_1m` **exposes no sum column at all**, so MSR-R2's forbidden aggregation is not something a dashboard can select by mistake, and `telemetry.flows_1m` exposes no `last_value`. The kind is stamped by the ingester from `MetricRegistry` and is **never read off the sample** (`crates/scm-ingest`), so an emitter cannot relabel its own metric; `encode_batch` drops an ungoverned metric rather than writing a blank kind. `apply.py`'s `VIEW_ASSERTIONS` fail if `levels_1m` ever grows a `sumMerge` or `flows_1m` an `argMaxMerge`, so the absence is gated and not merely intended. Two corrections came out of the fix: the aggregate is **`argMax(value, ts)`, not `anyLast`** — this row's own wording had named them as alternatives, and `anyLast` returns whichever row the engine merged last, which under parallel inserts is a nondeterministic pick that usually looks right — and no ADR was needed, because the fix changes nothing about what the product measures. Residual: the classification is a hand-maintained registry, so a *new* metric can still be entered under the wrong kind; that is narrower than a schema that made the wrong answer the only answer. | mitigated | any level metric proposed for ingestion; any new rollup stage; a new entry in the metric registry; the M4 dashboard work |
| 15 | **A per-event *duration* has no correct kind, and the nearest wrong one is addressable.** M2b's taxonomy is flow · level · event_count, which covers everything ingested today. It does not cover a duration observed per event — lead time for changes (CPT-0156), failed-deployment recovery time (CPT-0158), the numerator of flow efficiency (CPT-0160). Such a measure is not a level (it is not read at an instant and there is no "current" value between events) and not a flow (adding one change's 4 hours to another's 6 gives 10 latency-hours, which is not a lead time of anything). Classified `flow` — the only kind whose p95, min and max are the right summaries — it lands in `flows_1m`, where `total` becomes selectable: the same failure mode #14 just closed, one variant over. | medium | medium | orchestrator | nothing is exposed yet: none of these three metrics is ingested, and the registry has no duration entry, so the wrong classification cannot have been made in silence. The response is deliberately *not* to invent a fourth kind now — that would add a read surface (`durations_*` with p95 and no total) before any emitter exists to fill it, and ADR-0036's forward-only migrations make a guessed shape expensive to withdraw. It is recorded here so the decision is taken **when the first duration metric is proposed**, which is the moment the answer is cheap and informed, rather than discovered afterwards in a dashboard. | open | the first duration-valued metric proposed for ingestion; CPT-0156, CPT-0158 or CPT-0160 entering the registry; any fourth `MetricKind` variant |

