---
id: program-improvement-register
title: "Improvement Register — continuous improvement log"
type: program
owner: orchestrator
status: draft
since: 2026-07-19
updated: 2026-08-04
relations:
  - { type: part-of, target: index-program }
  - { type: governed-by, target: governance-root }
---
# Improvement Register

> The continuous-improvement loop, made a register (ADR-0012): when a task, incident or
> review teaches a **structural** lesson (about the process, the templates, the rules —
> not about one bug), it lands here and drives a change: a template edit, a new rule, an
> ADR, a gate. Rows are appended, never rewritten; a lesson without a resulting change is
> explicitly marked `accepted-as-is` with the reason.

## How to use

- A row is appended at task end (the self-review's "estimate honesty" and backlog
  follow-ups feed this register).
- The owner reviews open rows when planning; a row is closed only by the change it
  produced (link it) or by an explicit decision not to act.
- Lessons that change governing knowledge go through their normal path (ADR for
  decisions, supersession for rules) — this register points at the change, it is never
  the change itself.

## Register

| # | Date | Source | Lesson | Structural change | Status |
|---|---|---|---|---|---|
| 1 | 2026-07-19 | commit `a12c114` (pre-adoption) | the same formula duplicated in TS and Python silently diverged — duplicated logic without a shared oracle WILL drift | golden test vectors shared by both languages (backlog U8) | open |
| 2 | 2026-07-19 | first `npm install` of the repo (skeleton unification) | declared dev tooling was mutually uninstallable (`eslint ^9` + `@typescript-eslint ^7`) — nothing had ever verified the toolchain itself | dependency fix + committed lockfile + CI running the real commands (ADR-0013, U6) | done |
| 3 | 2026-07-19 | v0.2 skeleton audit | structural completeness ≠ model impact: exemplars, context budgets, recorded corrections and a fast gate move AI results more than added rules | ADR-0012 (exemplar, G9 budgets, pitfalls, verify split, handoff) | done |
| 4 | 2026-07-19 | cross-repo sync with the context-template skeleton | the instantiated gate script drifted from the skeleton's semantics: its rule-ID regex also matched the em-dash used by inherited-rule references (`templates/rule.md`), a latent G3 false-duplicate once per-department rule.md files land (U4) | regex aligned with the skeleton (definitions end in `:`); lesson: instantiate gate *configuration*, never gate *semantics* | done |
| 5 | 2026-07-27 | ADR-0037 (the owner stopping a wrong direction) | the estate had been building a *fictitious company* for weeks while every gate stayed green — because the gates checked internal consistency (links, IDs, symbol coverage) and nothing checked whether a statement was **externally fixed**. Green gates certified that the invented policy was well-organized. | the inclusion test at the head of `CLAUDE.md`; `SCM-R*` reclassified with a §Project decisions section; G10 rewritten from code coverage to **standards provenance**. Note the residual honestly: no gate can tell a standard from a plausible invention — that judgement is named as a reviewer's job, not automated away. | done |
| 6 | 2026-07-27 | PR #6 CI failure | `make verify-full` passed locally and CI failed on `pnpm install --frozen-lockfile`: a dependency was removed from a `package.json` without regenerating the lockfile, and the local gate never exercised the install step CI performs. **A gate that skips what CI does is not a gate.** | `deps-locked` target added to `verify-full`, running the exact CI invocation. Also removed two committed submodule gitlinks (`.claude/worktrees/…`) with no `.gitmodules`, which made every CI checkout emit a fatal error. | done |
| 7 | 2026-07-27 | Phase C4b (the skills/commands sweep) | **G11 shipped green while three retired rule IDs were cited in the skills tree** — it scanned only front-matter documents, and `.claude/**` is where the instructions that shape future work actually live. A gate over part of the estate certifies the part it can see, and the uncovered part is exactly where drift hides. Second lesson, from the same sweep: the two most-read files (`supply-chain-core/SKILL.md`, root `README.md`) were the two most stale — reading frequency and maintenance attention are unrelated. | G11 extended to `.claude/**` and `CLAUDE.md`; verified by planting a retired-ID citation. Both files rewritten. Dead Jest/eslint config and scripts removed rather than left declaring an estate that no longer exists. | done |
| 8 | 2026-07-27 | Phase C1d | **A statement attributed to two implementations escapes a policy sweep.** C1a searched for policy stated as policy; the same thresholds written as "PY bands / TS bands" passed, because a comparison reads as a description of code rather than a rule. When the implementations were then deleted, the numbers stayed — no longer describing anything, still reading as guidance. Second finding: `**FIN-R***` was cited 47 times as though it were a rule, invisible to every existing gate because it is not a broken link, not a duplicate and not a retired ID. | Both classes swept; **G12** added (a rule citation names an ID) and verified by planting a wildcard. Lesson for future sweeps: search for the *shape* of policy (a number next to a judgement) rather than for policy phrasing, and treat any comparison between implementations as a place where a decision is being deferred rather than recorded. | done |
| 9 | 2026-07-27 | Phase C5 | **The reference document was the stalest thing in the repository, and the catalogue that cites it was more current.** `REGULATORY_FRAMEWORK.md` published the superseded CSDDD phase-in, a 5% penalty cap that is now 3%, and no mention that the civil liability regime was deleted — while the concept node had already recorded the amendment. Nothing pointed the two at each other: a doc gate can check that a source is cited, never that the citation still says what the source says. Second finding: a node can record the current law in a footnote while its **Formula section still teaches the superseded test**, which is what a reader implements. | Framework rewritten with a **verification date per entry** and an explicit half-life warning; CPT-0093 restructured so current law leads and the old phasing is labelled do-not-implement. The residual is unchanged and unautomatable — someone has to re-read the instrument. Recorded as a standing review item rather than a one-off. | done |
| 10 | 2026-07-27 | Owner directive | **A refined prompt can still be underspecified, and the two default responses are both wrong.** PLT-R1 improves an incoming request but says nothing about what happens when a detail the owner never stated would change what gets built. Guessing produces something plausible that nobody chose — this estate inherited a USD 5,000 threshold, a 5% tolerance and a 40/30/20/10 weighting exactly that way — and asking in prose buries the decision in a paragraph that gets skimmed. | **ADR-0038 + PLT-R6:** the search for a better implementation is standing (inside adopted lanes only, never a new technology); a detail whose absence changes the work is presented as a **selectable list** with trade-offs and a stated recommendation; selections are built the same turn; declines are recorded here as `accepted-as-is` so they are not re-proposed. Question craft in `evaluation.md` §5. **Recorded limitation:** gated on ambiguity, the gate goes quiet when requests are well-specified — improvements found during clear work must be raised in the handoff report instead. | done |
| 11 | 2026-07-29 | The owner asking for a file-by-file review of the whole project | **A completed sweep and a clean estate are different claims, and only one of them was ever checked.** Four phases had swept for policy and each reported success; enumerating all 292 tracked files and marking them one by one found 53 still carrying something — including two thresholds Phase C5 had *reported in writing* and left in place, and the glossary publishing `OTD … World-class ≥ 95%`, the exact sentence `CLAUDE.md` names as the anti-pattern. The mechanism was not carelessness: without an enumerated estate nothing distinguishes *checked and clean* from *not reached*, so a sweep's own report becomes its only evidence. Second lesson, sharper: the biggest reservoir of the ADR-0037 defect was `.claude/skills/**`, which no gate reads and which every session loads — reviewing what is governed leaves the instructions untouched. | **`program/review-protocol.md`** — enumerate the estate mechanically and state the count, name the finding classes before looking, mark every item as reached, consolidate into the durable records, then fix / mechanize / raise as a selectable list; the checklist is transient and deleted at the close. Wired into `CLAUDE.md` §Working agreements and `evaluation.md` §5. Two classes made mechanical rather than hoped-for: **G8** now screens every tracked Markdown file for non-English function words (it was manual, and it had missed one Spanish sentence in `node-model.md` since the day the file was written) and **G13** checks that `updated:` matches the file's real last change (it disagreed in 164 of 221 documents). | done |
| 12 | 2026-07-29 | Three red CI runs on the commits that added G13 | **A gate must be verified against the environment that runs it, not the one that wrote it.** G13 was green locally and RED in CI three times, and I reported it as green from the local run alone — the exact shape of improvement #6 ("a gate that skips what CI does is not a gate"), inverted: this time the gate did something in CI that it never did locally. Two causes, both about how the code under test is *checked out* rather than about the check: `actions/checkout` clones at **depth 1**, git honours the shallow graft so HEAD reports **zero** parents, and `git show --name-only HEAD` therefore lists the entire tree — the gate demanded today's date on all 222 documents. On a `pull_request` event checkout uses the synthetic **merge ref**, whose first parent is the base branch, so HEAD's diff is every file the branch changes against base. The commit message asserted the design was shallow-clone-safe; the reasoning was right about `git log` and wrong about `git show`, and nothing had tested it. | `fetch-depth: 2` in the workflow, which is the minimum that makes the diff real. G13 now computes its scope from what is knowable and **says out loud when it cannot check** — `Gates.note()` prints an INFO line rather than letting a skip look like a pass, because a silent skip is how a gate reports success for work it never did. Verified against four real clones before pushing: depth-1 boundary (skips, with the reason), depth-2 (checks), a dirty unstamped change (fails), and a committed stale stamp at depth 2 (fails). **The standing rule this adds: a new gate is proven by planting a violation in the environment CI uses, not by reading its code.** | done |
| 13 | 2026-08-02 | Modelling a warehouse scorecard against the context (an exercise outside this repo) | **The same arithmetic identity was being rewritten per department, and the most important one was written nowhere.** Dozens of nodes define ratios — OTD, OTIF, fill rate, PPM, perfect order, bin accuracy — and **none said how to aggregate one**; the intuitive way is wrong and the error grows with the spread of the denominators (2/100, 3/10 and 1/90 give 3.0 % pooled and 11.0 % averaged). Looking for where to put it surfaced the pattern: QMS-R7 (opportunity base), RSK-R5 (an ordinal score stays ordinal) and four conservations (PRC-R4, SPL-R5, WHS-R5, ORD-R5) are **special cases of something general, restated for want of a common home**. The immediate symptom: the flow-versus-level argument had been duplicated verbatim across three warehouse nodes three days earlier, in this same repository. | **ADR-0039 plus the `30-foundation/measurement/` axis and the `MSR-R*` family**: MSR-R1 (a ratio aggregates from its components) and MSR-R2 (a level is never summed). Five warehouse nodes and two indexes now **cite** instead of repeating, and three of them shrank. QMS-R7 and RSK-R5 stay where they are, with one line connecting each to the general case. **The decision rule this leaves: when a statement appears a second time in two different places, the question is not which copy to fix but what general statement both are instances of.** An honest note on process: the *immediate* reason for opening a new axis was that `scm-core/rule.md` sat at 963 of its 1,000-word budget and two rules did not fit — but raising the budget would have been tuning a gate to fit the change instead of deciding where the change belongs, so the constraint served as a trigger and not as the justification. | done |
| 14 | 2026-08-02 | The owner asking for the branches to be put in order | **The repository's own movement had no rule, and the disorder was a state rather than a habit.** Twenty-two pull requests had all targeted `claude/bold-cannon-l7wtso` — a name a session generated, which became the default branch by accident — while **no `main` existed**, and three branches merged in July were still on the remote carrying **0 commits not already in the base**, verified with `git rev-list --count`. Risk #3 had named this since 2026-07-19 and it had stayed a note for two weeks because nothing said what *in order* meant. The sharper lesson is about the working branch: it has been reused across nineteen pull requests and that only works **because** it is restarted from the base each time. The first time it is not, the next pull request silently carries the previous one's commits and its diff stops being reviewable — an invariant the estate depended on and had never written down. | **ADR-0040 + ENG-R11**, six clauses: one long-lived line named `main`; work branches short-lived and restarted after each merge; merged means deleted; nothing left open across a turn boundary without the reason reported; the base current at merge time; **merge commits only, because G13 diffs `HEAD` against its parent and a squash would fail `updated:` on every file it carries** — the merge method and the gate turned out to be coupled, which is exactly the kind of link a rule exists to record. Two steps stay with the owner and are named rather than skipped: flipping the default branch, and deleting the stale refs (this environment's git proxy refuses `delete` with HTTP 403). **Process note, second occurrence this session:** ENG-R11 did not fit — the `ENG` family sat at its 1,000-word ceiling — so the rule was trimmed to the law and the evidence moved into the ADR, and restatement already in the file was removed to make room. The budget did the same job here as it did for MSR-R*: it forced the change into the shape it should have had anyway. **The family is now at 1000/1000; ENG-R12 cannot be added without splitting the file.** | done |
| 15 | 2026-08-02 | The owner asking for researched improvements, verified against what the repository is | **The thirteen gates guarding 230 documents had no tests, and the rule that would have caught it was already written down.** Improvement-register #12 states it plainly — *a gate is proven by planting a violation in the environment CI uses, not by reading its code* — and that proof had been performed **once, by hand, for G13**. Measured: `verify.py` at 638 lines, 0 tests, 0 per-gate functions, 1 `assert`. The next edit could break G4 in silence and every run would still print GREEN, which is the shape of ADR-0037 all over again: green gates certifying that something wrong is well-organised. **The lesson is not about testing.** A rule written into a register is not a mechanism; it is a hope that the next author reads the register. #12 was authored three days earlier by the same process that then failed to apply it. | **ADR-0042 + `tools/test_gates.py`**, in `make verify-full`: one planted violation per gate, asserting the gate fires **and that no other does**. End-to-end against a real worktree populated from the index, so a gate added in a change is tested in its presence. **It contradicted its author on its first green run** — the harness predicted a duplicated id would also trip G5, reasoning that the losing document's `part-of` chain would dangle; both documents carry `part-of` to the same index, so it does not. Plausible, and wrong, and caught in seconds. **The decision rule this leaves: when a register entry states a discipline, ask what mechanism performs it — and if the answer is *a person remembering*, the entry is not done.** | done |
| 16 | 2026-08-02 | Adopting gate G14 | **A budget set as a ratchet over an append-only document is a gate that punishes the practice it protects.** G14 prices what a session reads *together*; `planning` measured 32,009 words and the budget was set 3 % above it. Then the two ADRs adopting G14 and the mutation harness added 1,674 words to the ADR index and **the gate went red on the commit that introduced it**. The change was correct; the budget shape was wrong. The ADR index grows monotonically by design (ADR-0011 — history is never rewritten), so any tight ratchet on it fails the next time anyone records a decision, and a gate that obstructs correct work gets disabled rather than obeyed. | `planning` carries a **ceiling with a structural answer attached** — 36,000, and when it is next reached the answer is splitting the ADR bodies out (backlog X3), **not another raise**. The other four load sets keep their ratchets, because none contains an append-only document. **The general form: before setting a threshold on a quantity, ask whether the quantity is bounded, ratcheting or monotonic — the three need different mechanisms, and using a ratchet on a monotonic quantity schedules a false alarm.** Recorded because the same mistake is available every time a budget is added. **ANNOTATED, one commit later: it recurred.** `reviewing-the-estate` went 21 words over its ratchet — because it contains the improvement register and the risk register, both append-only, and **the row being added was this one**. Writing the lesson down did not apply it, which is improvement #15's point demonstrating itself inside the register that holds it. The mechanism that actually fixes it is not a number: every load set is now classified **ratchet or ceiling** in the manifest, by asking whether any member grows monotonically by design, and each ceiling names the structural answer it takes when reached (split the ADR bodies; archive closed rows) instead of a raise. | done |
| 17 | 2026-08-02 | Building the context-adherence evaluation (ADR-0043) | **The estate had fifteen ways to check itself and none to check whether it worked.** Every gate answers *is this document consistent with the others?* Nothing answered *does an agent reading this produce something conforming to it?* — the premise the whole repository rests on. Two design traps surfaced while closing it, and both are general. **First: the cheap subject is the useless one.** Self-evaluation by the session was the obvious way to get a number, and it measures the conversation rather than the context — a session that just authored a rule cites it from memory and scores 100 % that means nothing. **Second: reusing the existing checks was tempting and insufficient.** Scoring an answer by *does `verify.py` stay green* costs no new code and misses the only class that matters: an invented threshold with correct front-matter, a cited source and a compliant word count **passes all fifteen gates**. That is ADR-0037's defect exactly — well-structured and false — so the gates cannot be the whole check surface for the very failure they were tightened after. | **ADR-0043**: five tasks, one per failure class with real history here, answered by a **cold subagent** loaded only with the declared load set, scored by **gates plus per-task deterministic assertions**, never by a judge model. Recorded with a digest per context-defining file; **G15** fails when one changes afterwards and *notes that it cannot check* while no measurement exists. The checkers carry their own compliant/violating samples (`--self-test`), because an untested checker is ADR-0042's hole in a new place. **Two decision rules this leaves. (a) When choosing what produces the artefact under test, ask what the subject already knows that the artefact is supposed to teach — if the answer is 'everything', the measurement is of the subject, not the artefact. (b) A check suite tightened after a defect is evidence about that defect's *shape*, not proof of coverage: the same defect in a well-formed wrapper walks straight through.** | done |
| 18 | 2026-08-02 | The first context-adherence run (ADR-0043) | **The evaluation found more wrong with the estate than with the answers.** Five cold subagents scored 3/5, and the two failures were ordinary — one cited six retired rules, one overran the concept budget by 106 words. The interesting findings came from *building the scorer*. Deciding whether a cited rule is live meant parsing rule definitions, and the parser — copied from G3's — reported **`ENG-R8..R11` and `PLT-R1..R6` as undefined**. They are defined; the regex required the colon to follow the ID immediately and those ten carry an em-dash title first. **G3 uses that same regex for its uniqueness check, so a duplicate `ENG-R10` would have passed.** Then the first fix reintroduced improvement #4's exact defect — reading `- **SCM-R3** — …records: corrected` as a definition — and produced five false duplicates. The real discriminator is not *where the colon is* but **where the bold span closes**: a definition puts the colon inside the emphasis, a reference closes it after the ID. | Parser corrected in both `verify.py` and `tools/context_eval.py`, with the regression written and run before it could be committed. **A second G3 mutant added**, because G3 asserts three things — unique document ids, unique rule IDs, unique ADR numbers — and only the first had a planted violation, which is exactly why the hole survived ADR-0042. **The rule this leaves: one mutant per gate is not one mutant per claim. Count what a gate asserts, not how many gates there are.** And the corollary that made this worth the effort: **a checker built to grade output is a second reader of the same rules, and it disagrees with the first for real reasons.** | done |
| 19 | 2026-08-02 | Scoring the first eval run | **A checker cannot tell a citation of a defect from a commission of it, and the compliant answers are the ones that quote defects.** Two of the five answers first scored FAIL for doing exactly the right thing: `invent-a-threshold` **refused** to state a tolerance and, explaining why, quoted `CLAUDE.md`'s own anti-pattern `"a 5% receipt tolerance"`; `unit-codes` gave `KGM`/`LTR`/`MTR` and warned that `KG` and `L` are invented shorthand. Both were flagged for containing the very strings that prove they understood. This is the **mirror image of risk #11** — that no gate can tell a standard from a plausible invention — and it bites harder in an evaluation, because the best answers are the most likely to name the failure they are avoiding. | Reported speech is now excluded before matching (quoted spans and code spans stripped), and a unit code on a line that disowns it is not read as used. Both false positives became **permanent regression samples** in `--self-test`, so a field-found miscall becomes a test rather than a patch nobody re-runs. **The general rule: a checker that searches for the shape of a defect will fire on text that names the defect, and in a corpus about avoiding defects that text is concentrated in the correct answers.** | done |
| 20 | 2026-08-02 | Writing up the first eval run | **Naming a defect trips the detector for that defect — and it happened three times in one day, in three different places.** (a) The `invent-a-threshold` answer was failed for quoting `CLAUDE.md`'s own anti-pattern while refusing to commit it. (b) The `unit-codes` answer was failed for warning that a shorthand is invented. (c) Then **G11 failed the report itself** for listing the retired rule IDs the run had caught — a gate that reads every tracked Markdown file cannot distinguish a citation-as-law from a citation-as-evidence, and the write-up of a violation contains the violation. | (a) and (b) fixed in the checker with reported speech excluded and regression samples added. **(c) was not fixed, and that is the decision worth recording**: G11 is right and the report is what changed. The measurement now states the count and the families and points at the command that prints the IDs, because weakening a gate so a document about the gate can be written is how gates get hollowed out. **The general form: any check that searches for the shape of a defect will fire on the text that documents it, and documentation of defects is where such text concentrates. Prefer changing the document; only change the check when the document cannot say what it needs to.** | done |
| 21 | 2026-08-02 | Acting on the first eval run | **The evaluation earned its keep on its first cycle: it named a defect, the fix was cheap, and the fix was *verified* rather than assumed.** `rule-citation` failed by citing six retired rules, and the diagnosis was not "the agent was careless" — **52 rules are retired, the id-registry enumerated 12**, and the retirement tables live in fifteen `rule.md` files the agent's load set did not contain. It could not have known. | A machine-readable **retired roster** in the registry — fifteen lines, the complete set — plus **G16** asserting it equals the union of the retirement tables **in both directions**, so it can neither fall behind nor claim a retirement nobody made, with a mutant for each direction. The load set was **not** widened: adding fifteen rule files would have cost ~8,000 words against a G14-priced budget, and a retirement is an allocation fact, which is the registry's own remit. **Then a fresh cold subagent, same load set, same prompt: PASS** — it read the roster and wrote "do not cite …" for ten retired IDs. **The rule this leaves: when an agent gets something wrong, ask what it was given before asking what it did. A failure that traces to an absent input is a manifest defect wearing an agent's clothes — and the cheapest fix is usually to make an existing document in the set carry the missing fact, not to add a document.** | done |
| 22 | 2026-08-02 | G15 firing on the write-up of its own measurement | **A freshness gate keyed to whole-file digests cannot tell a material change from a cosmetic one.** Adding a gate-list line to `CLAUDE.md` invalidated a five-task measurement as loudly as rewriting a rule would. The alternatives are each worse: dates cannot be trusted in a shallow clone (G13 paid three red CI runs for that), and a section-level hash would require a section vocabulary nothing else in the estate uses. | **Not engineered around — stated.** The measurement record now says exactly which task was scored against which state of the tree, instead of implying all five saw the same one. **The general form: a staleness check is only as precise as the unit it hashes, and widening the unit to something cheap to compute buys false alarms. When the false alarms are cheap to explain and the alternative is an unverifiable date, prefer the false alarm and write down what it means.** Second, smaller lesson from the same day: **five separate times, a check fired on text that named the defect it looks for** — three in the eval checkers, twice in G11 against the write-up. That is now a documented class with a shared helper and a stated threshold: if it needs a sixth widening, the line-level regex is the wrong instrument. | done |
| 23 | 2026-08-03 | X5, and the eval's second closed loop | **A rule the agent had read, in a file it had loaded, was still broken — because the estate had no form that turns a rule into an order of operations.** `new-concept-node` produced a structurally perfect node at 806 words against a 700-word budget stated plainly in front of it. The instinct was to state the budget more loudly; that instinct is wrong, because repetition was not the gap. Against Diátaxis's four forms the estate had *reference* (182 nodes, 20 rule files) and *explanation* (the ADRs) and **nothing task-oriented at all**, while the first line of `CLAUDE.md` promises a project learns "which departments it needs **and how to implement them**". **And the reason the form never existed is the interesting part:** the obvious how-tos — how to receive goods, how to score a supplier — are method an organization can reasonably choose, so they fail the inclusion test. The forbidden instances made the whole category look forbidden. | **ADR-0044**: `how-to` joins the type vocabulary at 900 words, scoped to *using this context* rather than running a department, with that constraint written next to the type in `knowledge-architecture.md` §8 rather than buried in the ADR. First guide targets the measured failure and makes the budget operational — count while writing, here is the command, trim the node not the budget, here is what to cut first. **Re-run: 633 words, budget named in the answer.** **The rule this leaves: when a rule is stated, loaded and still broken, the gap is a missing *form*, not insufficient emphasis — and when a whole category of document is missing, check whether its most obvious instances are the forbidden ones.** | done |
| 24 | 2026-08-03 | Scoring the X5 re-run | **A check that delegates to a whole suite inherits every verdict the suite reaches, including the ones that are not about the thing under test.** `new-concept-node` scores a candidate by placing it in a worktree and requiring `verify.py` green — which is the right design, and it meant the re-run scored FAIL on **G15 reporting that the measurement record was stale**. True, and nothing whatever to do with the candidate node. The delegation was borrowed for its strength (the task cannot drift from the gates) and inherited a scope nobody chose. | The check now attributes only failures **naming the candidate file**, which keeps cross-file verdicts that genuinely implicate it — G10's duplicate-CPT message names both files, so a stolen number is still caught. **The general form: delegation transfers authority over a *decision*, not over a *scope*. When you hand a verdict to a broader instrument, say which of its findings are yours to answer for.** | done |
| 25 | 2026-08-03 | Acting on my own recommendation from the improvement dossier | **A recommendation written from research and not re-checked against the estate named work that was already done.** The handoff proposed adding "Rework Rate + Change Failure Rate" to the platform delivery metrics. **CFR has been CPT-0157 since Phase M1.** Enumerating the group first — six files, one command — showed four of DORA's five metrics present and told me which one was actually missing. Worse, the same enumeration found a second defect the recommendation had not seen at all: the catalogue's **grouping** was outdated, reading as "two pairs" where the published taxonomy is three throughput measures and two instability ones. | CPT-0167 added; grouping corrected with the trade-off argument preserved; cross-links both ways. **The rule this leaves, and it is the review protocol's own rule applied to advice instead of documents: enumerate the estate before recommending an addition to it. A recommendation is a claim about what is missing, and "missing" is a fact about the estate, not about the source you read.** The dossier was right about the gap class and wrong about the instance, which is the failure mode of researching outward and reporting inward without a pass in between. | done |
| 26 | 2026-08-03 | Allocating CPT-0167 | **A test that names a mutable identifier is a landmine with a delay fuse.** The `new-concept-node` eval task tells a cold subagent to author a node numbered CPT-0167 — free when the prompt was written that morning, and allocated to a real node the same afternoon. From then on the task would have failed on a duplicate CPT: a spurious failure, in the one place the estate measures whether it works, and one that would have looked like a context regression. | **CPT-0999 reserved in the ID registry and never allocated**, with the reason recorded there because that file is the collision authority. **The general form: anything a test asserts about identifiers must use identifiers the allocation authority has set aside. A test drawing from the same pool as production work is only passing until the pool reaches it.** Noted alongside: writing this change's node hit the 700-word budget twice (777, then 723) — the failure the eval had just measured in an agent, reproduced by its author within the hour, which is the argument for the how-to being operational rather than emphatic. | done |
| 27 | 2026-08-03 | The owner asking for a plain summary of the project | **The estate's weakest point was not rigour — it was saying what it is for, and no mechanism could have found that.** Sixteen gates, an eval, forty-four ADRs, and the entry point opened by naming a supply-chain knowledge base, then named a DevOps monitoring application, and **never connected them**. A reader could obey every rule and miss the point. The connection existed — in ADR-0030, one decision among forty-four, exactly where someone looking for *purpose* would not look. **And the eval could not have caught it:** all five tasks measured whether an agent *obeys* the context; none asked whether it can say what the context is **for**. The instrument was blind to the class the owner found by asking a question. | **ADR-0045**: three paragraphs at the entry point (two axes, the portfolio, why monitoring), the practice-area roster for the engineering axis, and **PLT-R7** — governing knowledge is selected and declared to the owner before development. Plus a sixth eval task, `what-is-this-for`, which is the part that matters: prose with nothing checking it drifts back. Verified by a cold subagent on the three always-loaded files: **PASS**. **Two rules this leaves. (a) A body of knowledge needs its purpose at its entry point, not in its decision log — a decision record explains *why a choice was made*, never *what the thing is*, and the two get confused because both feel like documentation. (b) An evaluation built from past failures can only test the classes that already failed; ask periodically what question the corpus cannot answer, because that is where the next defect is.** | done |
| 28 | 2026-08-03 | Allocating PLT-R7 and CPT-0167 | **Third occurrence of the ratchet-over-monotonic-quantity mistake, and this time the rule for it was already written.** `changing-a-rule` went 157 words over its ratchet because it contains the id-registry, which grows with every allocation and never shrinks. Improvement #16 had established the classification (ratchet for bounded members, ceiling for append-only ones) and #16's own annotation had already recorded a *second* occurrence — but the rule was applied to the two sets that failed and **never swept across the sets that already existed**. | `changing-a-rule` reclassified as a ceiling with its structural answer named (compact the CPT allocation prose, which lists every range in words). **The rule this leaves, and it is improvement #15 for the fourth time: a classification rule is not applied by being written — it is applied by sweeping every existing instance the day it is written.** Fixing the cases that failed and leaving the rest is how a rule becomes a rule that only applies to new work. | done |
| 29 | 2026-08-03 | M2b — implementing the mitigation risk #14 prescribed | **A mitigation column is a plan, and a plan can be wrong in a way no gate reads.** Risk #14 said the fix needed "`argMaxState`/`anyLastState` in the cascade", naming the two as alternatives, and the backlog entry copied that wording forward. They are not alternatives: `anyLast` returns whichever row the engine merged last, so under parallel inserts and background merges it is a nondeterministic pick that *usually* matches the newest reading and diverges precisely under load — the failure mode is invisible in a test and appears in production. The register had carried the error for two days across two documents, because a mitigation column is prose that no gate can evaluate, and quoting it into the backlog gave it a second appearance that read as corroboration. | **A prescribed mechanism is re-derived at implementation time, not executed as written** — and when it turns out wrong the risk row is corrected in place with the reason, so the next reader does not inherit the mistake. Both rows now say `argMax`, and the migration header states why in the file a future contributor will actually open. | done |
| 30 | 2026-08-03 | M2b — choosing between documenting an invalid aggregation and removing it | **An operation that must never be performed should be *unaddressable*, not documented as forbidden.** The plan was to add `argMaxState` alongside `sumState` so that a level "aggregates by last and never by sum" — an instruction to whoever writes the query, which is the same class of control as the KPI target tables risk #13 is about: correct, prominent, and dependent on being read. What shipped instead splits the read surface so `telemetry.levels_1m` has **no sum column in it at all**. The two designs cost the same migration; only one of them survives a contributor who never reads the comment. | The pattern is named in `db/clickhouse/README.md` and given a gate: `apply.py`'s `VIEW_ASSERTIONS` take a boolean, so a check can assert a substring is **absent** from a view definition and fail when someone helpfully adds it. Generalizes beyond this schema — wherever the estate currently *warns* against an operation, the question to ask is whether the operation can be removed from the surface instead. | done |
| 31 | 2026-08-03 | Looking for archivable rows in this register, to take a load-set exit | **Fourteen consecutive rows of this register had no `Status` cell, and the estate was green for two days.** A Markdown row missing its trailing cell renders as an *empty* cell — nothing looks broken — and the field that was missing is the one the register's own instructions use to decide whether a lesson is closed. Found only because a word budget forced a search for `done` rows and the search could not find them. The concepts index had the mirror image: a third column with an **empty heading** that one of fifteen rows filled. Neither is a typo class; both are a structural claim ("this table has these fields") silently unmet. | **G17** — every table row carries the cell count its header declares, over every tracked Markdown file, with a mutant in `test_gates.py`. The splitter honours `\|`, because a node writing `\|ME\|` for an absolute value is correct and a gate that called it a defect would be the third instance of pitfall *a check fires on the text that names its own subject*. **The rule this leaves: when a table encodes a claim about its own fields, the claim is checkable — and 'it renders fine' is not evidence, because a short row renders fine by design.** | done |
| 32 | 2026-08-03 | G17 pushing `every-task` 10 words past its ratchet | **Fifth occurrence of the ratchet-over-monotonic-quantity class — and the first the rule caught instead of the gate.** Improvement #16 established the test and #28 established that it must be swept; both were then applied as *does this set contain an append-only **file***. Every set contains `CLAUDE.md`, whose **gate roster is append-only** — gate IDs are fixed and new gates append — so no set was ever legitimately a ratchet, and the one with a single word of headroom went red on a correct change. Related, same day: the pitfall list was first appended to `evaluation.md`, which broke three sets at once because that file belongs to four of them. | Both remaining ratchets reclassified as ceilings with their exits named (compress the roster to gate names; §11 already carries the descriptions in full). **The rule this leaves, and it is the correction to #16 rather than a repetition of it: ask the monotonic question of *sections*, not of documents. A roster of identifiers inside a bounded document is an append-only part, and the enclosing file's boundedness says nothing about it.** Second rule, from the `evaluation.md` misplacement: **before adding words to a document, check how many load sets contain it** — placement is a budget decision even when it does not feel like one. | done |
| 33 | 2026-08-03 | Re-running the context-adherence evaluation after M2b | **Sixth occurrence of "a check fires on the text that names its own subject" — and the register had already written down the threshold for stopping.** Improvement #22 said: if this needs a sixth widening, the line-level regex is the wrong instrument. It did. A **correct** `unit-codes` answer failed on all four codes: it gave `KGM`/`LTR`/`MTR`, quoted `CLAUDE.md`'s anti-pattern (which spells out the invented `KG`, `L`, `M`), and named a fourth code only to refuse to assert it — and wrapped prose had put every disowning word on a *different line* from its token. The heuristic could not see across a line break, and no widening fixes that: the distinction between asserting a code and mentioning one is not carried by a sentence in any form a program can read. | The task now asks for its conclusion in a fenced ``answer`` block, one `quantity: CODE` line each, and **only that block is scored**; prose may quote and warn freely. The failing answer is a permanent `--self-test` sample. **The rule this leaves: a stated threshold is only worth writing if it is honoured on the occurrence that hits it — and the occurrence will always arrive looking like one more small widening.** Same day, same shape as G17: when a claim must be checkable, give it a structure instead of inferring it from prose. | done |
| 34 | 2026-08-03 | The same regression, looked at a second time | **The checker was not the only thing wrong, and fixing it would have hidden the real defect.** `unit-codes` was declared against the `every-task` load set, which carries **no unit-code list**: three of the four codes appear there only as an illustration inside an anti-pattern bullet, and the fourth appears nowhere at all. Two separate cold runs answered as well as that set allows, and the previous cycle recorded one of them as a **PASS** — which was luck, not conformance. A red gate invites fixing the instrument and stopping, and that is exactly what nearly happened here. | A new load set, `recording-a-quantity`, carrying the regulatory reference whose code table answers all four; the task re-declared against it; re-run clean with `EA` included. **The rule this leaves: a task can only be scored against a set that can answer it, and ADR-0043 already said the manifest is what is wrong when it cannot** — this is improvement #21 (*ask what the agent was given before asking what it did*) paying for itself a second time. **And the meta-rule: when a check fires, finish diagnosing after the first cause is found.** One defect explaining the symptom is not evidence that it is the only one. | done |
| 35 | 2026-08-03 | Sweeping the estate for references to files that no longer exist | **Thirteen of the fourteen department skills still directed an agent at code ADR-0037 deleted, and `procurement` — the one that had been fixed — is what made the other thirteen visible.** A scan for backticked repository paths that resolve to nothing returned 121 mentions across 30 files. Sorting them cost more than finding them: **59 were false positives** from resolving relative citations against the repo root instead of the citing file's directory, and **~45 of the true residue are legitimate** — an ADR that records deleting `src/departments/` must name it, and `_index.md` sections marked *reserved* or *MISSING (owner input needed)* are honest about what is not there. What was left was one class: **live guidance, loaded by every session, listing `domain/NCR.ts` and `services/QualityService.ts` as though a reader could open them.** Same reservoir as #11 — `.claude/skills/**`, which no gate reads — and the same reason it survived: the ADR-0037 sweep deleted the code and corrected the governed documents, and the instructions that pointed at the code were neither. | All 14 skills now carry one heading and one disclaimer: *shapes, not code — a project builds these in its own repository, with its own policy values and its own layout*, and the directory prefixes are stripped because the layout was never this repository's to imply. `procurement` was unified onto the same wording rather than left as a fourteenth variant. **CPT-0063** was the other find: a concept node whose whole content was a rounding difference between two deleted Python files — it now states the thing that is actually true independent of any implementation, that the denominator is the *inspected* quantity, with the fiftyfold error that follows from using the received quantity instead. **Not mechanized, and deliberately:** a path-existence gate would now fire on all 62 corrected shape names, and a gate that reddens correct work gets disabled rather than obeyed (#28). The gate needs the disclaimer convention to be machine-readable first; raised as a decision, not half-built. | done |
| 36 | 2026-08-03 | The owner asking for a file-by-file review of the whole project | **Two decisions were cited as law while no decision existed, and the gate that should have caught it only looked one way.** `ADR-0045` and `ADR-0046` had one-line index entries and **no `## ADR-NNNN` body** — 46 entries against 44 bodies — while **PLT-R7** named ADR-0045 as its authority, `templates/knowledge-selection.md` cited ADR-0046 twice, and the ADR index itself marked the versioning question closed "→ ADR-0046". G9 checked **body → entry** and never **entry → body**, so green meant nothing in that direction. Second find, same shape: **four concept nodes published an invented scoring scheme as their canonical formula** — CPT-0025's 0.8×/1.5× bands and 90-day window, CPT-0080's 30/35/25/10 weighting with 180/365-day bands and GREEN≥75 cut-offs, CPT-0132's whole ESG point system, CPT-0138's 0.6/0.4 blend. Two of them *said in prose* the numbers were policy and published them in the `## Formula` section anyway, which is the 40/30/20/10 anti-pattern `CLAUDE.md` names, surviving four sweeps in the nodes those sweeps did not reach. **Third: G15's watched list is hand-written and had fallen behind its own manifest** — `how-to/change-a-rule.md` joined the `changing-a-rule` set two commits earlier and was never added, so `rule-citation` was scored against an input whose change could not register. | **G9 now checks both directions** and carries a third mutant (20 gates, one per claim). **G15 now derives its watched set from the manifest** and fails when a declared member is unwatched — the drift that produced it cannot recur. The two ADR bodies are written. The four nodes state the **shape** parametrically (`k_low`/`k_high`, `Σwᵢ = 1`, β) with the numbers moved into `Project-chosen inputs` and each row naming what constrains it; worked examples keep the numbers, labelled as the example's own. Also fixed: a duplicated intro paragraph in `known-pitfalls.md` citing a `review-protocol.md` section that does not exist, a sentence fragment glued onto CPT-0025's DMD-R9 citation by an unread scripted edit, a stale `58 of 154 nodes` in the ID registry (really 71 of 167), ADR-0011 reading `Decision (proposed)` under an Accepted status with no note that ADR-0046 killed its SemVer clause, and ADR-0012 still describing G1–G7+G9 and a single CI invocation. **The denominator matters: 322 files enumerated, 12 carried findings.** | done |
| 37 | 2026-08-03 | The `invent-a-threshold` evaluation failing a correct answer, for the second time | **A fix that targets the spellings of a thing instead of the thing lasts exactly one cycle.** On 2026-08-02 this checker failed an answer that *refused* to state a tolerance and quoted `CLAUDE.md`'s own anti-pattern list to explain why; the fix was `QUOTED_SPAN`, covering `"…"`, `“…”` and `` `…` `` — the three quotation syntaxes that answer happened to use. One cycle later an equally correct answer quoted the same anti-pattern in a **Markdown blockquote**, with the disowning sentence wrapped onto a line the number did not share, and failed again. The checker is line-scoped, so it saw a bare `5% receipt tolerance` on an undisowned line. **`DISOWNS` already carries a written threshold** saying that if a fifth widening is needed the line-level regex is the wrong instrument — and adding a word for this would have been that widening in spirit, on a different code path. | A **Markdown blockquote is now reported speech**, alongside `QUOTED_SPAN`: structure rather than vocabulary, which is what the threshold was pointing at. `invent-a-threshold-blockquote` is a permanent regression sample carrying the legitimate case and a violating one, so the fix cannot be narrowed back (9 checkers, all discriminating). The evasion it admits — policy asserted inside a blockquote — is written into the code and the record rather than left to be discovered. **The transferable form: when a false positive recurs, ask whether the previous fix named the concept or an instance of it.** | done |
| 38 | 2026-08-03 | `rule-citation` failing a correct answer, hours after #37 fixed the same class elsewhere | **Three correct answers have now failed the same way, and each fix was local until the third.** The mechanism never changed: the checkers are **line-scoped**, and an answer that disowns an ID puts the disowning word on another line as soon as the paragraph wraps. `rule-citation` rejected a line that named a **retired core rule as "the old" one and gave its live successor** — a write-off using the registry's replacement table exactly as intended, with *retired* one line up. (The identifier is elided: spelling it out made **G11** fail this register, the same gate-versus-quotation collision one layer up, and the estate's remedy is to change the document rather than weaken the check. It is in the regression sample, which G11 does not read.) The three fixes had been: widen a word list (four times), add three quotation syntaxes, add blockquotes. **The instrument was the defect, and `DISOWNS` already said so** — its comment names the remedy for this very checker: *ask for a structured answer instead of scoring free prose.* It had been written and not applied, which is improvement #15's shape (a rule in a register is a hope, not a mechanism). | `rule-citation` now ends with a declared ```answer block holding the IDs it endorses, and **only that block is scored** — the shape `unit-codes` has carried since its own regression. Prose outside is free, which is the point: discussing a retired ID or a rejected candidate is good practice that the old form punished. Two samples migrated rather than replaced so nothing is lost, plus `rule-citation-writes-off-retired` as the permanent record (10 checkers, all discriminating). The trade is stated: a missing block now fails as non-compliance instead of a false accusation. **The transferable form: when the same class recurs a third time, stop fixing instances and read whether an earlier author already named the instrument.** | done |
| 39 | 2026-08-03 | `invent-a-threshold` failing a correct answer for the fourth time — one turn after #37 and #38 recorded the lesson | **I applied the instance fix I had just finished writing down as the mistake.** #37 fixed this checker by adding Markdown blockquotes to the reported-speech test, and #38 stated the rule *when a false positive recurs, ask whether the previous fix named the concept or an instance of it.* The very next answer disowned the value in **plain past-tense prose** — *“that 5% receipt tolerance was deleted for this reason”* — and failed. Four occurrences, four correct answers, and the fix history is a list of quotation *shapes*: a word list widened four times, three quote syntaxes, blockquotes. Every one of them treated the symptom, and the fourth was mine, written after the register already said not to. | The task now ends with a declared ```answer block holding the values it asserts as binding, with `none` as the compliant answer, and **only that block is scored** — the same instrument `unit-codes` and `rule-citation` already carry. Three samples migrated into one that holds every shape which used to fail (blockquote, inline quote, past-tense prose), so the family cannot regress. **The general rule this settles is worth more than the fix: an evaluation task whose failure class is *asserting something that should not be asserted* is scored on a declared block, never on prose.** Exactly three tasks have that shape and all three now do it; `level-metric` and `what-is-this-for` check for the *presence* of correct reasoning and are unaffected. The pattern to notice next time: three consecutive fixes that each add one more way of writing the same thing is the signal that the scan itself is wrong. | done |
| 40 | 2026-08-04 | A pre-M4 readiness check, asked before starting the phase rather than after | **Two findings, and the first is that I reported a blocker that was not one.** Asked what M4 lacks, I read the ADR index and ADR-0020 — a stale `Accepted` decision prescribing a Python gRPC calculation core — and concluded no live decision covered the NestJS↔Rust boundary. **ENG-R10.1 had named it all along:** *transport lives in adapters (`napi-rs` toward NestJS, `tonic` toward Python)*, with ENG-R10.5 fixing the call direction. **A decision recorded as a rule clause is invisible to a search of the decision log**, and the decision log is where one looks for decisions. **The second finding is worse and was found only because I went back to read the rule properly.** ENG-R10.7 said a `pub fn` implementing a concept *is linked from that node (G10)* — while G10's third check fails any node carrying an `## Implementations` section, and ADR-0037 §3 removed that section from every node. ADR-0035 wrote the clause on 22 July; ADR-0037 reversed its premise on the 29th. **For six weeks a live rule ordered the exact thing a gate rejects, and cited that gate as its enforcer.** No gate can catch a semantic contradiction between prose and code. | ADR-0020 superseded. ENG-R10.7 rewritten to *Citation is one-way* at 999/1000 words, the `ENG` family having no headroom. The readiness check itself is kept in the backlog **with the wrong finding struck through rather than deleted**, because the correction is the useful part. **Two transferable rules: when looking for whether something is decided, search the rule families and not only the ADR index; and when a decision reverses a premise, grep the rule text for clauses built on it** — ADR-0037 swept the nodes and the docs and did not sweep the rules that described them. Nothing was built on either error: the check ran before the phase, which is the whole argument for running it there. | done |
| 41 | 2026-08-04 | A Context-OS proposal, audited before any code | **The proposal asked to improve a retrieval system this repository does not have, and the audit found a worse problem than the one it targeted.** Measured: zero embeddings, zero vector store, zero LLM SDK, zero caches, `dependencies: {}`. Then the number that mattered: **the six declared load sets reach 17 of 241 governed documents, with zero concept nodes of 167 and zero department rule files of 14** — a session authoring a node got the template and two foundation rules and no example, while **559 typed edges** already stated what every node depends on and were read by nothing. **Nine of the proposal's 28 requirements were already satisfied deterministically** (graph, context diff, claim extraction and verification, token accounting, semantic memory), and rebuilding them with a model would have been a regression — ADR-0043 rejected an LLM judge on recorded bias grounds. | **ADR-0050**: retrieval becomes graph-derived, the artefact is a resolver rather than a pipeline, and the retrieval stack is deferred with a stated condition (a corpus where traversal from a declared root cannot reach what a task needs). `tools/context_set.py` assembles and prices what a session will actually open, closing G14's gap between pricing the *declaration* and pricing the *session*; G14 learned the `graph:` selector and carries a mutant for a dangling id (24 mutants). **The resolver immediately indicted a budget:** `authoring-a-concept` at 8,200 buys no knowledge at all, and aiming it at one real node costs 9,147. Recorded in the manifest without raising the number, because the point is that the trade is now visible per session. **The transferable rule: audit what exists before accepting a proposal's framing.** Two thirds of this one was greenfield described as improvement, and the estate's real weakness was in the reach of what it already had. | done |
| 42 | 2026-08-04 | Noticing that a watched file's digest already matched, and asking why | **Self-inflicted, and it is the one way the measurement record can lie.** ENG-R10.7 was corrected inside `50-engineering/rule.md`, a member of `changing-a-rule` and therefore an input to `rule-citation` — a **material** change. The re-run was launched, did not land, and the digest was then refreshed **in a blanket loop over every watched file**. The block went green while the recorded measurement no longer described its input: exactly the state G15 exists to forbid, reached by satisfying the gate mechanically rather than by ignoring it. A per-file digest cannot distinguish a refresh that *follows* a re-run from one that *replaces* it. **Found by noticing the digest already matched and asking why, not by any check.** | The task was re-run cold and passed, declaring SCM-R9 and SCM-R10. **The rule: refresh a digest only for the file whose task you have just scored** — a loop over the whole block is a convenience that converts the freshness claim into a formality. Recorded beside the row it affected in the measurement document, because the failure is invisible in green output and only legible next to the result. **Row 41 also shipped a literal `\n` in its last cell** from a scripted append that was not read back — the pitfall this register already carries, committed into the register itself. Fixed here. | done |
| 43 | 2026-08-04 | Building ADR-0050's remaining checks, and being failed a fifth time by my own newest checker | **Five correct answers have now been rejected by these checkers, and the four earlier fixes each treated a symptom of one cause: they read `splitlines()`, and prose wraps.** A claim and the word disowning it land on different lines the moment a paragraph runs past the margin. The fifth was `it is **not** itself a` / `supply-chain product` — a denial split by the wrap. The fix history reads as a list of increasingly specific patches: a word list widened four times, three quotation syntaxes, Markdown blockquotes, then a declared `answer` block per task. **Every one was a real improvement and none was the cause.** Worse, I had written the generalisation in #39 — *a check for 'asserted something it should not' reads a declared block* — and classified `what-is-this-for` as exempt because its dominant shape is presence-checking, missing that it also carries a forbidden-assertion clause. | **The unit of analysis is now the paragraph** — split on blank lines, wrapped lines joined — on the principle that *a line is an artefact of wrapping and a paragraph is what someone wrote*. The same change fixed a **false negative** sitting beside it: the `connected` check demanded that one *line* tie monitoring to the projects, so a sentence making the connection while wrapping would have been reported as never making it. Regression sample carries the exact wrapped shape. **A sixth finding came from writing that sample:** phrased in CLAUDE.md's own words — *and to engineer software well* — it was rejected for never naming the engineering axis, because the vocabulary held `engineering` and not `engineer software`. Widened, with the distinction stated: widening a **positive presence** vocabulary lowers false negatives and cannot create a false accusation, which is the opposite of widening a defect-shape pattern. | done |
| 44 | 2026-08-04 | Measuring the estate against an external reference model instead of against a proposal | **A proposal can only surface the gaps its author thought of.** The Context-OS audit (#41) answered 28 requirements and concluded, correctly, that two thirds were greenfield. Building a four-pillar reference model — context engineering, RAG, memory, agentic AI — from current practice and scoring the estate against it found **six gaps the proposal never raised**, including the one that matters most: the engineering axis rosters thirty-five practice areas and **none of them is the discipline this repository runs on**. Every project in the portfolio will be built the way this repository is built, and the axis whose purpose is to state how software is engineered was silent about it. It was also *anchorable all along* — six published anchors, so it was never a consensus area, merely an unrostered one. | ADR-0053 adds practice area #36 with its anchors. `docs/program/agentic-context-assessment.md` records the model, the 33-capability matrix (21 have · 5 partial · 6 gap) and the four declared non-goals with their reversal conditions. Risk #16 opens the agent-plane threat surface the matrix exposed. | done |
| 45 | 2026-08-04 | The dossier, read before deciding, being wrong about six counted facts at once | **A status document is believed long after it stops being true, and the ungated numbers borrow authority from the gated ones.** `state-of-the-project.md` said 154 concept nodes (167), thirteen gates (twenty), ADRs 0001–0036 (fifty-one), and carried a *known inconsistency* section describing a sweep closed five days earlier. Nothing watched any of it, in an estate where twenty other properties are gated. **The instrument is drift, not a calendar** — a wall-clock staleness check reddens correct work in a quiet week, and #16's lesson is that such a gate gets disabled rather than obeyed. | **G21** (ADR-0052) recomputes eleven declared facts; an unknown key fails so interpretation cannot wear the typography of a measurement, a measurable key left undeclared fails so the block cannot omit the inconvenient number, and `snapshot` must equal `updated:`. `GATE_NAMES` moves to module scope in `verify.py`, so the gate count has one home. Four mutants, one per claim. | done |
| 46 | 2026-08-04 | Taking a load set's declared structural exit and still being over budget | **Compression is a one-time payment on a recurring bill.** `authoring-a-concept` breached its ceiling when G21 was added. The manifest's exit — *§11 keeps the descriptions, CLAUDE.md keeps only the names* — was executed in full, and the set was still 17 words over, because halving the copies does not stop the surviving copy growing and the growth is structural: a gate ID is fixed forever and a retired gate stays listed. The ratchet/ceiling rule said which sets grow; it did not say what to do when the growing *part* is what breaches one. | The roster moved to `docs/00-governance/gates.md`, a file **no load set carries**: §11 became a pointer, `CLAUDE.md` kept the names a session needs, 8,217 → 7,745, and every future gate now costs that set nothing. `load-sets.md` records the exit as taken-and-insufficient, with the general form: **move the growing part out of every member; do not compress it and do not raise the number.** | done |
| 47 | 2026-08-04 | Three mutant collateral declarations going wrong in one commit, with no gate having changed | **A declared collateral can encode a fact about *measurement state*, not only about the file set — and then it drifts the moment that state moves.** Five digests went back to `(unmeasured)` after the roster relocation, so touching a watched file no longer invalidates anything; three `also` columns that had been correct for a week reported *gates failing to fire*. This is #36's lesson — a hand-written list mirroring a machine-readable one will drift — arriving in the one place that exists to prove the gates behave. | `tools/test_gates.py` **derives** the G15 collateral from the digest block instead of being told it. What stays declared is the coupling that is genuinely state-independent: G14's two mutants add a manifest member, so G15's watched-set-coverage claim fires whether or not the manifest is measured — and the comment now says which half is which. | done |
| 48 | 2026-08-04 | Measuring the external-content surface before designing a guard for it | **The cheapest guard is one on a property that is already true.** The plan for risk #16 assumed a sweep: find the laundered claims and clean them. Measurement first found **zero external URLs in the governed estate and one in the whole tracked tree** — the estate cites by author, work and clause, because a name does not rot. So there was nothing to sweep, and the correct instrument was a gate that **guards the absence**: green on the day it lands, and it stops the property becoming false. Designing before measuring would have produced a sweep with no subject and no gate. | **G22** (ADR-0054): an external URL is declared in the file's `external-sources` block with its retrieval date, or absent — both directions, three mutants. Scope is every tracked file, because the one real URL lives in `.claude/**`. The prohibition in knowledge-architecture §7 carries the two undecidable clauses, and says which is which. | done |
| 49 | 2026-08-04 | Looking for somewhere to put one new rule | **A per-file word budget over an append-only roster is a ratchet, and G9's `rule` budget was never classified.** `platform/rule.md` measures **1000 of 1000** words and `50-engineering/rule.md` **999** — the two families that could host an agent-plane rule — and **neither has a retirement table to move out**, while `scm-core` is at 1000 with 583 of it retired and the fourteen departments sit at 470–700 with about half retired. This is the same class as #16 and #32, third appearance, now in **G9** instead of G14: a rule family only grows, so the next rule in either family is blocked today. | **Named, not worked around.** The rule went to knowledge-architecture §7, which is its correct home and carries no budget, so nothing was forced into the wrong family to dodge the number. ADR-0054 records the constraint in its consequences and its alternatives; the two structural answers — move the retirement tables to one register (frees all sixteen families, touches G11 and G16) or create the pre-declared `security/` axis (needs owner authorization per `30-foundation/_index.md`) — are a backlog item with the measurement attached. | open |
