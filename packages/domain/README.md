# @scm/domain

The innermost Clean-Architecture ring (ADR-0018): the 14 SCOR-DS department domains,
framework-free (ENG-R2). Moved here from `src/departments` at the monorepo restructure
(ADR-0023). Depends only on `@scm/shared`. Consumed by `packages/application` use-cases.
