# @scm/infrastructure  (scaffold — Stage A/C)

Adapters: Postgres persistence, the event-store adapter (ADR-0005), the gRPC calc client
(ADR-0020), the knowledge-graph read-model ingester (ADR-0024). Empty until P3/P7.
