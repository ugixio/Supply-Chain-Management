# Monorepo workspaces (ADR-0022/0023). Activation (pnpm install + lockfile migration
# from package-lock.json, CI switch to `pnpm install --frozen-lockfile`) is the P2
# activation sub-step; until then the TS toolchain resolves @scm/* via tsconfig paths.
packages:
  - "apps/*"
  - "packages/*"
