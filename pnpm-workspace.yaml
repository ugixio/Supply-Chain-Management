# Monorepo workspaces (ADR-0022/0023). Active: `pnpm-lock.yaml` is committed and
# `make verify-full` runs `pnpm install --frozen-lockfile`, the exact invocation CI performs.
# `apps/*` holds only READMEs today — the monitoring application is Phase M4.
packages:
  - "apps/*"
  - "packages/*"
