# proto  (scaffold — gRPC contracts)

Protobuf contracts for the Python calc service (`scm.calc.v1`, ADR-0020). Money and rate
fields are `string`, never `double` (ENG-R5). Populated at Stage B (P6).
