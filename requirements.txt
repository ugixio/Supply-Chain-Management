# Supply Chain Management — Python Dependencies
# All libraries are OSI open source licensed
# Install: pip install -r requirements.txt

# ── Numerics & Data ─────────────────────────────────────────────────────────
numpy>=1.26.0           # BSD-3-Clause
scipy>=1.12.0           # BSD-3-Clause
pandas>=2.2.0           # BSD-3-Clause

# ── Statistics & Forecasting ─────────────────────────────────────────────────
statsmodels>=0.14.0     # BSD-3-Clause  (ARIMA, ETS, regression, SPC)
prophet>=1.1.5          # MIT           (Facebook/Meta time-series)
statsforecast>=1.7.0    # Apache-2.0    (SMA, ETS, ARIMA at scale)

# ── Machine Learning ─────────────────────────────────────────────────────────
scikit-learn>=1.4.0     # BSD-3-Clause  (classification, clustering, anomaly)
xgboost>=2.0.0          # Apache-2.0    (gradient boosting)
lightgbm>=4.3.0         # MIT           (gradient boosting, fast)

# ── Deep Learning ────────────────────────────────────────────────────────────
torch>=2.2.0            # BSD-3-Clause  (PyTorch — LSTM, Autoencoder, GNN)
torchvision>=0.17.0     # BSD-3-Clause
tensorflow>=2.16.0      # Apache-2.0    (alternative deep learning)

# ── Reinforcement Learning ───────────────────────────────────────────────────
stable-baselines3>=2.3.0  # MIT         (PPO, DQN, SAC)
gymnasium>=0.29.0          # MIT         (RL environments, replaces gym)

# ── Optimization ─────────────────────────────────────────────────────────────
pulp>=2.8.0             # MIT           (linear programming)
ortools>=9.9.0          # Apache-2.0    (VRP, scheduling — Google OR-Tools)

# ── Graph ────────────────────────────────────────────────────────────────────
networkx>=3.3.0         # BSD-3-Clause  (graph analysis, HHI, cascade risk)
torch-geometric>=2.5.0  # MIT           (Graph Neural Networks)

# ── NLP ──────────────────────────────────────────────────────────────────────
transformers>=4.40.0    # Apache-2.0    (BERT, DistilBERT — HuggingFace)
spacy>=3.7.0            # MIT           (NLP pipeline, NER)
pdfplumber>=0.11.0      # MIT           (PDF text extraction)
pytesseract>=0.3.10     # Apache-2.0    (OCR wrapper for Tesseract)

# ── Computer Vision ──────────────────────────────────────────────────────────
ultralytics>=8.2.0      # AGPL-3.0      (YOLOv8 object detection)
opencv-python>=4.9.0.80 # Apache-2.0    (image processing)
Pillow>=10.3.0          # HPND (OSI)    (image I/O)

# ── Geospatial / Satellite ───────────────────────────────────────────────────
rasterio>=1.3.10        # BSD-3-Clause  (raster satellite imagery)
geopandas>=0.14.0       # BSD-3-Clause  (geospatial data)
shapely>=2.0.0          # BSD-3-Clause  (geometry operations)

# ── Simulation ───────────────────────────────────────────────────────────────
simpy>=4.1.1            # MIT           (discrete event simulation / Digital Twin)

# ── Search / Graph DB alternative ────────────────────────────────────────────
opensearch-py>=2.5.0    # Apache-2.0    (replaces Elasticsearch SSPL)

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.2.0           # MIT
pytest-cov>=5.0.0       # MIT

# ── Utilities ────────────────────────────────────────────────────────────────
python-dateutil>=2.9.0  # Apache-2.0
pytz>=2024.1            # MIT
pydantic>=2.7.0         # MIT           (data validation, mirrors TypeScript types)
