# calc  (Python calculation core)

All mathematical models and ML (ADR-0001), moved here from `python/` (ADR-0023). Exposed
to the API as a gRPC service (`scm.calc.v1`, ADR-0020); money/rates cross as strings
(ENG-R5). Keeps `requirements.txt` (Python packaging, ADR-0001).
