# pytest configuration — OSI: pytest (MIT)
# Enforced slice (U7): `make test-py` runs `services/calc/tests` (stdlib-only money core).
# The full model suite (numpy/torch/…) is run outside CI until a heavier lane exists.
